export const resolvers = {
  Query: {
    // getUserById: (parent,args,context) => context..
  },

  // Mutation: {
  //   createUser: async (parent, { args }, { me, models }) => {},
  // },
};

// Typescipty

// type vs interface

// class , getters , setters

// implements an interfact

// OOPS using typescripts
